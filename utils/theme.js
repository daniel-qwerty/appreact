import { DefaultTheme } from 'react-native-paper';

export const theme = {
   ...DefaultTheme,
 roundness: 15,
  colors: {
    ...DefaultTheme.colors,
    primary: '#F07DA8',
    accent: '#212121',
    
  },
};