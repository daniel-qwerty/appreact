import { DefaultTheme } from 'react-native-paper';

export const theme = {
   ...DefaultTheme,
 roundness: 15,
  colors: {
    ...DefaultTheme.colors,
    primary: '#FD91B9',
    accent: '#4A4849',  
  },
};